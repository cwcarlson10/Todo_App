<%= form_for @todo do |f| %>
	<%= f.label :title %>:
	<%= f.text_field :title %>

	<%= f.label :category %>
	<%= f.text_field :category %>

	<%= f.label :priority %>
	<%= f.text_field :priority %>

	<%= f.label :is_completed %>
	<%= f.check_box :is_completed %>

	<%= f.submit %>
<% end %>
